## AI session — Full CRUD for grades, attendance, dashboard

### Rust Backend
- `commands/grades.rs`: `get_grades` (pivoted matrix with quiz_ids/assignment_ids for inline editing), `create_quiz_bulk`/`create_assignment_bulk`, `update_quiz_score`/`update_assignment_score`, `delete_quiz_column`/`delete_assignment_column` (delete entire column by name+date)
- `commands/attendance_cmd.rs`: `create_lecture`, `get_lectures`, `delete_lecture`, `get_attendance`, `mark_attendance` (upsert), `seed_attendance` (create Absent for all enrolled)
- Migration 012: added `semester_year_id` to lectures

### Frontend
- **Grades page**: Full grade sheet with quizzes/assignments as separate tables, inline editing (click cell → input → Enter/blur saves), hover-to-delete column headers with confirm, grand totals table
- **Attendance page**: Split panel — lecture list (left) + attendance roster (right). One-click Present/Absent/Excused toggle. Seed roster button creates records for all enrolled students
- **Dashboard**: 4-card stats (students/quizzes/assignments/lectures) + getting-started hints. Filter-aware.

### Commits
- `4ee3324` — grades CRUD (quizzes + assignments with inline editing)
- `da5515e` — attendance CRUD + dashboard stats
